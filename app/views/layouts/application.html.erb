<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Quilla Alma" %></title>

    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="site-header">
      <div class="site-header__inner">
        <%= link_to "Quilla Alma", root_path, class: "brand" %>

        <nav aria-label="Main navigation">
          <%= link_to "Products", products_path %>
          <%= link_to "Categories", categories_path %>
          <%= link_to "Cart (#{cart_item_count})", cart_path %>

          <% if customer_signed_in? %>
            <%= link_to "My Orders", orders_path %>
            <%= link_to "My account",
                        edit_customer_registration_path %>

            <%= button_to "Log out",
                          destroy_customer_session_path,
                          method: :delete,
                          class: "nav-button" %>
          <% else %>
            <%= link_to "Create account",
                        new_customer_registration_path %>

            <%= link_to "Log in",
                        new_customer_session_path %>
          <% end %>

          <%= link_to "Admin", admin_root_path %>
        </nav>
      </div>
    </header>

    <main class="site-main">
      <%# Displays feedback after cart actions. %>
      <% if notice.present? %>
        <div class="flash flash--notice" role="status">
          <%= notice %>
        </div>
      <% end %>

      <% if alert.present? %>
        <div class="flash flash--alert" role="alert">
          <%= alert %>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <footer class="site-footer">
      <p>Quilla Alma — Ecuadorian-inspired goods in Winnipeg.</p>
    </footer>
  </body>
</html>
